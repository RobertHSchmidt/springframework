# properties defined in this file are overridable by a local build.properties in this project dir

# The location of the common build system
common.build.dir=${basedir}/../../common-build

# Overriding the broken ivyconf.xml in common-build, until it's fixed
ivy.conf.dir=${basedir}

project.base.version=1.1.1-dev
#project.version=${project.base.version}
#ivy.status=release

javac.source=1.3
javac.target=1.3
